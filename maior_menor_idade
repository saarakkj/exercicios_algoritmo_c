#include <stdio.h>
#include <locale.h>
#include <conio.h>

main () {

 int n, qtde=0, vezes;

 setlocale(LC_ALL, "portuguese");
 for(vezes=0; vezes<50; vezes++) {

 printf("\nInforme o %2d° idade: ", vezes+1);
 scanf("%d", &n);

 if (n >= 18)
    printf("maior de idade");
 else if (n < 18)
    printf("menor de idade");
 }
}
